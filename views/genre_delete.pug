extends layout

block content
  h1 #{title}: #{genre.name}
  p= genre.lifespan

  if genre_books.length

    p #[strong 이 장르를 삭제하시려면 아래 책들을 먼저 삭제하여 주세요.]

    div(style='margin-left:20px;margin-top:20px')

      h4 포함된 책 리스트

      dl
      each book in genre_books
        dt
          a(href=book.url) #{book.title}
        dd #{book.summary}

  else
    p 이 장르를 삭제하시겠습니까?

    form(method='POST' action='')
      div.form-group
        input#genreid.form-control(type='hidden',name='authorid', required='true', value=genre._id )

      button.btn.btn-primary(type='submit') 삭제하기